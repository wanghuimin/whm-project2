#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.3.5.6@mocha-phantomjs/bin/mocha-phantomjs" "$@"
  ret=$?
else 
  node  "$basedir/../../../.3.5.6@mocha-phantomjs/bin/mocha-phantomjs" "$@"
  ret=$?
fi
exit $ret
