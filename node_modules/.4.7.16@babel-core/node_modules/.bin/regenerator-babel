#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.0.8.13-2@regenerator-babel/bin/regenerator" "$@"
  ret=$?
else 
  node  "$basedir/../../../.0.8.13-2@regenerator-babel/bin/regenerator" "$@"
  ret=$?
fi
exit $ret
