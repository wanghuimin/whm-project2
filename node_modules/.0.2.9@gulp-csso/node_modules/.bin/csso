#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.1.3.12@csso/bin/csso" "$@"
  ret=$?
else 
  node  "$basedir/../../../.1.3.12@csso/bin/csso" "$@"
  ret=$?
fi
exit $ret
